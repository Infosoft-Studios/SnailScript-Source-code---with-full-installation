#include <windows.h>
#include <raylib.h>
#include <iostream>
using namespace std;

namespace __main__classes__
{
    class controlhw
    {
        protected:
            void __re__main__()
            {
                WINCLASS wc;
                wc.IndexedValues                = GetIndexedValues(nullptr, 90);
                wc.getlocaldata                 = GetRedoIndexedBit32(32, 64& nullptr);

                switch (!wc.getlocaldata)
                {
                    case wc.getloadedata:
                        wc.loadata              = RedoInt(nullptr);
                        break;
                
                    default:
                        break;
                };

                wc.getredodata                  = LoadInit(nullptr);
            }
    };
};
