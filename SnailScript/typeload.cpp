#include <windows.h>
#include <raylib.h>
#include <iostream>
using namespace std;

void useingsysten()
{
    WINCLASS wc;
    wc.loaderindexed              = LoadIndexing(nullptr);
    if (!msg) { return wc.loaderindexed; };
};

// end 
